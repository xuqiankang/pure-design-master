import Vue from 'vue'
import Vuex from 'vuex'
import getters from './getters'

// 导入模块

const state = {}
const actions = {}
const mutations = {}

Vue.use(Vuex)
export default new Vuex.Store({
  modules: {
  
  },
  state,
  actions,
  mutations,
  getters
})
